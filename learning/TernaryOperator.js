// operator always produce a value = expressions

const age = 45;
age >= 12 ? console.log("yeah"):("sorry");


const eligible = age >= 12 ? 'okay' : 'not satisfied';

console.log(eligible);
// const tip;
// const testData;
const value=0;
if ((value === 50 && 300) ? '15%' : '20% ');